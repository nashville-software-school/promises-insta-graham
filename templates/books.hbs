{{#each books}}
  <div class="col-md-12">
    <h3>{{ title }}</h3>
    <h4>{{ author }}, {{ year }}</h4>
    <p>{{ synopsis }}</p>
    <footer>
      <h5>{{ type }}</h5>
      <h6>Publisher : {{ publisher }}</h6>
    </footer>
  </div>
{{/each}}